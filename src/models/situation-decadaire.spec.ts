import { SituationDecadaire } from './situation-decadaire';

describe('SituationDecadaire', () => {
  it('should create an instance', () => {
    expect(new SituationDecadaire()).toBeTruthy();
  });
});

