import { GestionAbonnementModule } from './gestion-abonnement.module';

describe('GestionAbonnement', () => {
  it('should create an instance', () => {
    expect(new GestionAbonnementModule()).toBeTruthy();
  });
});





