import { UtilisateurRegister } from './utilisateur-register';

describe('UtilisateurRegister', () => {
  it('should create an instance', () => {
    expect(new UtilisateurRegister()).toBeTruthy();
  });
});
