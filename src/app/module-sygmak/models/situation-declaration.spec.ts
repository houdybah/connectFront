import { SituationDeclaration } from './situation-declaration';

describe('SituationDeclaration', () => {
  it('should create an instance', () => {
    expect(new SituationDeclaration()).toBeTruthy();
  });
});
